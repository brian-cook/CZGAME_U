namespace CZ.Core
{
    /// <summary>
    /// Core game state enum used across the game
    /// </summary>
    public enum GameState
    {
        MainMenu,
        Playing,
        Paused,
        GameOver
    }
} 